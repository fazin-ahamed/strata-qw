interface:
  display_name: "Strata Qw"
  short_description: "Repo-specific patterns and workflows for strata-qw"
  default_prompt: "Use the strata-qw repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true